"""
Internal implementation modules for `tools/webapp/app.py`.

`tools/webapp/app.py` remains the public/stable facade; this package exists to keep the
legacy module from growing unmanageably large.
"""
