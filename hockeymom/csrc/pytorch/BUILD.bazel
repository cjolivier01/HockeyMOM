load("@rules_cuda//cuda:defs.bzl", "cuda_library")

cc_library(
    name = "pytorch_stitch",
    srcs = glob(["*.cpp"]),
    hdrs = glob(["*.h"]),
    copts = [
        "-fopenmp",
        "-fPIC",
        "-Wno-error=comment",
    ],
    includes=[
      ".",
    ],
    visibility = ["//visibility:public"],
    deps = [
      "@libtorch",
      "@local_cuda//:cuda_runtime",
    ],
)
