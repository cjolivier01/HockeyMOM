aspen:
  minimal_context: true

  plugins:
    # Assumes an existing pipeline already defines 'actions' trunk producing
    # per-frame action_results. This example only wires a saver.
    save_actions:
      class: hmlib.aspen.plugins.save_plugins.SaveActionsPlugin
      depends: [actions]
      params: {}

